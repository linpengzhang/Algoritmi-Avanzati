import heapq

def heapify(x):
    return heapq.heapify(x)

def heappop(heap):
    return heapq.heappop(heap)

def heappush(heap, item):
    return heapq.heappush(heap, item)

# def decreasekey(heap, pos, new_key):
# ...come determinare la posizione per chiamare il metodo?   
